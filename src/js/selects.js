const customSelects = function () {
    

    // const selects = document.querySelectorAll('.js-custom-select');

    // const customSelects = [...selects];

    // customSelects.map(select => {
    //     console.log(select);

    //     $(select).select2({
    //         width: '100%',
    //         minimumResultsForSearch: -1
    //     });
    // })

    // const selectsAdv = document.querySelectorAll('.js-custom-select-adv');

    // const customSelectsAdv = [...selectsAdv];

    // customSelectsAdv.map(select => {
    //     console.log(select);

    //     $(select).select2({
    //         width: '100%',
    //         minimumResultsForSearch: -1,
    //         theme: 'adv'
    //     });
    // })
}

export default customSelects;